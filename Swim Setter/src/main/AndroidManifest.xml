<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tygrdevelopment.swimsetter.swimsetter.swimsetter" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.tygrdevelopment.swimsetter.swimsetter.swimsetter.Overview"
            android:label="@string/app_name"
            android:theme="@style/AppBaseTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.tygrdevelopment.swimsetter.swimsetter.swimsetter.WorkoutLoader"
            android:label="@string/title_activity_workout_loader"
            android:theme="@style/AppBaseTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="Overview" />
        </activity>
    </application>

</manifest>
